{
  "name": "ci-cd-tutorial",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
     "format:check": "prettier --check \"**/*\" --ignore \"**/.github/**\""
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "prettier": "^3.7.4"
  }
}
